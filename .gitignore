# API keys — never commit
NomLens/Config.xcconfig

# Trained model weights — delivered via OTA, not stored in repo
*.mlpackage
*.mlmodelc

# Xcode
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods (not used, but just in case)
Pods/
